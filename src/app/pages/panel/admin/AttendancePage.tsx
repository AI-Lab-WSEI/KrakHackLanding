/**
 * /panel/admin/obecnosc — attendance tracking (confirmed / pending / declined)
 * Widoczne: admin. Komponent `AdminAttendance` reużyty z `pages/AdminAttendance.tsx`.
 */
import { AdminAttendance } from '@/app/pages/AdminAttendance';

export function AdminAttendancePage() {
  return <AdminAttendance />;
}
